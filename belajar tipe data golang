package main

import "fmt"

func main(){

/*
tipe data pada golang:
1. Tipe Data Numerik Non-Desimal
    Tipe data numerik non-desimal atau non floating point di Golang ada beberapa jenis.
    Secara umum ada 2 tipe data kategori ini yang perlu diketahui.
    * uint , tipe data untuk bilangan cacah (bilangan positif).
    * int , tipe data untuk bilangan bulat (bilangan negatif dan positif).

Kedua tipe data di atas kemudian dibagi lagi menjadi beberapa jenis, dengan pembagian
berdasarkan lebar cakupan nilainya, detailnya bisa dilihat di tabel berikut.

Tipe data Cakupan bilangan
uint8 0 ↔ 255
uint16 0 ↔ 65535
uint32 0 ↔ 4294967295
uint64 0 ↔ 18446744073709551615
uint sama dengan uint32 atau uint64 (tergantung nilai)
byte sama dengan uint8
int8 -128 ↔ 127
int16 -32768 ↔ 32767
int32 -2147483648 ↔ 2147483647
int64 -9223372036854775808 ↔ 9223372036854775807
int sama dengan int32 atau int64 (tergantung nilai)
rune sama dengan int32

2. Tipe Data Numerik Desimal
    * float32
    * float64
3. Tipe Data bool (Boolean)
Tipe data bool berisikan hanya 2 variansi nilai, true dan false


*/
ada := true
	fmt.Printf("ada? %t\n", ada) // %t untuk memfomat data boolean(true&false)

	pesan := "eel"
	fmt.Printf("hallo %s", pesan + "!")

	// Nilai nil dan nilai default tipe data
	/*nilai defaultnya string " " (string kosong)
	  nilai defaultnya bool adalah false
	  nilai default tipe numerik non-desimal adalah 0
	  nilai default tipe numerik desimal adalah 0.0
	  
	  nil tidak bisa dipakai di tipe data, ada tipe data yang dapat diset nilainya dengan nil
	  - pointer
	  - tipe data fungsi
	  - slice
	  - map
	  - channel
	  - interface kosong / interface{} */
	










}
